#+ ===== Declaracion de variables ===== +#
init {
    a, b, e, f, g, h : Int;
    c, d, i, j : Float;
    nombre, apellido : String;
    fecha : Date;
}

#+ ===== While con ciclo anidado ===== +#
while (a < 10) {
    write("Incrementando a");
    a := a + 1;
}


while (a < 10) {
    write("Incrementando a");
    a := a + 1;
    while (a < 10) {
        write("Incrementando a");
        a := a + 1;
    }
}
